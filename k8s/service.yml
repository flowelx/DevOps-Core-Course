apiVersion: v1
kind: Service
metadata:
  name: python-app-service
  namespace: default
  labels:
    app: python-app
    tier: backend
spec:
  type: NodePort
  selector:
    app: python-app
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30080
    protocol: TCP
    name: http